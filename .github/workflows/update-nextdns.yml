name: Update NextDNS Denylist

on:
  schedule:
    - cron: '0 2 * * *' # Auto-updates every single day at 02:00 UTC
  workflow_dispatch: # Allows you to manual-run anytime from GitHub Actions tab

permissions:
  contents: read

jobs:
  update-denylist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install nextdnsctl engine
        run: pip install nextdnsctl

      - name: Merge and Download Blocklists
        run: |
          LISTS_FILE="blocklists.txt"
          if [ ! -f "$LISTS_FILE" ]; then
            echo "::error::blocklists.txt file not found!"
            exit 1
          fi

          COMBINED_RAW="combined_raw.txt"
          touch "$COMBINED_RAW"

          # Automatically loop through any URLs you put inside blocklists.txt
          while IFS= read -r url || [ -n "$url" ]; do
            url=$(echo "$url" | xargs)
            [[ -z "$url" || "$url" =~ ^# ]] && continue
            echo "Downloading list: $url"
            curl -sfL "$url" >> "$COMBINED_RAW" || echo "Warning: Failed to fetch $url"
          done < "$LISTS_FILE"

          # Deduplicate, normalize, and strip out comments natively
          sed 's/#.*//' "$COMBINED_RAW" | sed 's/^[[:space:]]*//; s/[[:space:]]*$//' | grep -v '^$' | sort -u > final_blocklist.txt
          echo "Processed total unique domains: $(wc -l < final_blocklist.txt)"

      - name: Dynamic Parallel Sync to NextDNS accounts
        env:
          NEXTDNS_ACCOUNT_1_API_KEY: ${{ secrets.NEXTDNS_ACCOUNT_1_API_KEY }}
          NEXTDNS_ACCOUNT_2_API_KEY: ${{ secrets.NEXTDNS_ACCOUNT_2_API_KEY }}
          NEXTDNS_ACCOUNT_3_API_KEY: ${{ secrets.NEXTDNS_ACCOUNT_3_API_KEY }}
          NEXTDNS_ACCOUNT_4_API_KEY: ${{ secrets.NEXTDNS_ACCOUNT_4_API_KEY }}
          NEXTDNS_ACCOUNT_5_API_KEY: ${{ secrets.NEXTDNS_ACCOUNT_5_API_KEY }}
        run: |
          account_num=1
          while true; do
            secret_name="NEXTDNS_ACCOUNT_${account_num}_API_KEY"
            api_key_value=${!secret_name}

            if [ -z "$api_key_value" ]; then
              echo "Finished checking all configured accounts."
              break
            fi

            echo "Syncing Account Index: $account_num"
            nextdnsctl auth "$api_key_value"

            # Dynamic check: get your alphanumeric profile IDs matching the account
            # (Fixes the hardcoded bug so it finds your active profiles automatically)
            PROFILES=$(nextdnsctl profile-list | awk '{print $1}' | tail -n +2)

            for profile in $PROFILES; do
              echo "Synchronizing profile: $profile"
              
              # CRITICAL SPEED TWEAK: nextdnsctl uses parallel processing by default. 
              # --update-existing ensures duplicate checking and prunes missing entries.
              nextdnsctl denylist import "$profile" final_blocklist.txt --update-existing
            done

            account_num=$((account_num + 1))
          done
